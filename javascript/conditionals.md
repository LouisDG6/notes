## if else statement
if(insert statement){
    insert function
  } else {
   insert function
  }

## ternary operator
condition ? express if true : express if false
Ex. 
var age = 26;
var beverage = (age >= 21) ? "Beer" : "Juice";
console.log(beverage); // "Beer"